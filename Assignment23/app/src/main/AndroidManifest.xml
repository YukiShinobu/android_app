<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.edu.utas.username.assignment2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment2">
        <activity android:name=".AddStudent"></activity>
        <activity android:name=".Display" />
        <activity android:name=".Add_student" />
        <activity android:name=".Student_Details" />
        <activity android:name=".InsideStudentPages" />
        <activity android:name=".InsideStudentPageDisplay"/>
        <activity android:name=".TotalSummaryPageDisplay"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>